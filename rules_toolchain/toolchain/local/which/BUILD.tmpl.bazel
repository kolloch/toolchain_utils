load("@rules_toolchain//toolchain:defs.bzl", "toolchain_local_binary")

package(default_visibility = ["//visibility:public"])

toolchain_local_binary(
    name = "{{name}}",
    path = "{{path}}",
    program = "{{program}}",
    variable = "{{variable}}",
)
