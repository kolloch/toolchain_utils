load("@rules_toolchain//toolchain/local:defs.bzl", "binary")

package(default_visibility = ["//visibility:public"])

binary(
    name = "{{name}}",
    path = "{{path}}",
    program = "{{program}}",
    variable = "{{variable}}",
)
