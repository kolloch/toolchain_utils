module(
    name = "rules_toolchain",
)

bazel_dep(name = "bazel_skylib", version = "1.4.2")
bazel_dep(name = "platforms", version = "0.0.7")

host = use_extension("//toolchain/host:extension.bzl", "host")
host.triplet(
    name = "host",
)
use_repo(host, "host")
