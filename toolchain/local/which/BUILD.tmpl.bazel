load("@rules_toolchain//toolchain:defs.bzl", "toolchain_symlink_path")
load("//:resolved.bzl", "resolved")

package(default_visibility = ["//visibility:public"])

toolchain_symlink_path(
    name = "{{name}}",
    path = "{{path}}",
    tags = ["no-remote"],
    variable = "{{variable}}",
)

resolved(
    name = "resolved",
    toolchain = "{{toolchain_type}}",
)
